# Resume goes here

Drop your PDF at:

```
public/resume/partha-das-resume.pdf
```

This filename matches `resume` in `src/data/portfolio.js`. If  you use a
different filename, update that config value to match. If the file is
missing, the "Download Resume" button still renders (browsers will show
a 404 on click) — replace this README with your actual PDF before
deploying.
